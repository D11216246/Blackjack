# 21 點（Blackjack）單檔網頁版 — 軟體開發規格（Spec）

## 1. 目的（Purpose）
以 **單一檔案 `index.html`** 實作一個可在瀏覽器直接開啟的 21 點（Blackjack）小遊戲，提供：
- 基本 UI（顯示玩家/莊家手牌、點數、狀態、操作按鈕）
- 隨機抽牌（使用瀏覽器內建亂數）
- 簡單敘述（Log/提示文字，描述每次發牌與結果）

本規格以「一般常見規則」為基礎並刻意簡化，以利快速完成可玩的 demo。

---

## 2. 範圍（Scope）
### 2.1 版本範圍（In Scope）
- 單人玩家 vs 莊家（Dealer）
- 一副牌（52 張），每局開始洗牌
- 初始牌：玩家 2 張明牌；莊家 1 明 1 暗（暗牌於玩家回合結束才翻開）
- 玩家操作：**Hit（要牌） / Stand（停牌） / New Game（新局）**
- 莊家行為：**≤16 必須要牌；≥17 停牌**
- 點數計算：
  - 2~10：面值
  - J/Q/K：10
  - A：1 或 11（以不爆牌且點數最大為優先）
- 勝負：
  - 玩家爆牌：輸
  - 莊家爆牌：玩家贏
  - 皆未爆：比點數，大者贏、同點 Push
- Blackjack（可選但建議做）：
  - 玩家起手兩張為 A + 10點牌 → Blackjack
  - 本 demo 不做賠率/下注，只需在 log 顯示並直接結算（若莊家也 Blackjack → Push）

### 2.2 不在範圍（Out of Scope）
- 下注、籌碼、派彩倍率（3:2 / 6:5）
- Double / Split / Insurance / Surrender
- 多副牌、滲透率、發牌鞋
- 多玩家、帳號系統、持久化儲存、排行榜
- 音效、動畫（可選加分但非必要）

---

## 3. 交付物（Deliverables）
- `index.html`（**唯一檔案**），內含：
  - HTML（UI 結構）
  - CSS（基本樣式，可置於 `<style>`）
  - JavaScript（遊戲邏輯，可置於 `<script>`）
- 本規格文件：`Spec.md`

---

## 4. 使用者故事（User Stories）
1. 身為玩家，我可以按「New Game」開始新局並看到初始牌。
2. 身為玩家，我可以按「Hit」抽一張牌，系統顯示新牌與我目前點數。
3. 身為玩家，我可以按「Stand」結束回合，系統翻開莊家暗牌並自動補牌至規則停止，最後顯示勝負。
4. 身為玩家，我可以從「Log」看到每一步動作的簡短敘述（發到什麼牌、是否爆牌、最後結果）。

---

## 5. 功能需求（Functional Requirements）

### FR-1：新局（New Game）
- 點擊 `New Game`：
  1. 生成一副 52 張標準撲克牌
  2. 亂數洗牌
  3. 清空舊狀態與 log
  4. 發牌：
     - 玩家：2 張明牌
     - 莊家：2 張（第 1 張明牌、第 2 張暗牌）
  5. 顯示：
     - 玩家 2 張牌 + 點數
     - 莊家明牌 + 「隱藏牌」佔位（不顯示點數或只顯示明牌點數，實作自行選一種但需一致）
  6. 若玩家或莊家起手 Blackjack（建議支援）：
     - 若玩家 Blackjack 且莊家非 Blackjack：顯示玩家勝
     - 若雙方 Blackjack：Push
     - 需立即進入結算狀態並停用 Hit/Stand

**驗收**
- 重新開始後不可沿用上一局牌堆/手牌
- UI 與 log 明確呈現目前局面

---

### FR-2：要牌（Hit）
- 玩家點 `Hit`：
  1. 從牌堆抽 1 張加入玩家手牌
  2. 更新玩家點數顯示
  3. 在 log 追加敘述：「玩家要牌：XX」
  4. 若玩家爆牌（>21）：
     - 立刻顯示結果「玩家爆牌，莊家勝」
     - 翻開莊家暗牌（可翻或不翻；建議翻開以清楚呈現結果）
     - 禁用 Hit/Stand

**前置條件**
- 遊戲在玩家回合中（非結算狀態）
- 玩家尚未爆牌

**驗收**
- 每次 Hit 恰好增加 1 張牌，且牌不重複（同一局內）

---

### FR-3：停牌（Stand）
- 玩家點 `Stand`：
  1. 玩家回合結束
  2. 翻開莊家暗牌並顯示莊家點數
  3. 莊家補牌規則（自動迴圈）：
     - 當莊家點數 **<=16**：抽 1 張並寫入 log：「莊家要牌：XX」
     - 當莊家點數 **>=17**：停止
  4. 結算勝負並顯示於結果區與 log

**驗收**
- 莊家自動抽牌直到規則停止
- 結果符合規則（玩家/莊家爆牌、點數大小、Push）

---

### FR-4：點數計算（A=1/11）
- 手牌點數計算需符合：
  - 先將 A 全部視為 1
  - 若存在 A，且「將其中一張 A 從 1 調整成 11」不會爆牌（+10），則可視情況調整，使點數最大但不超過 21
- 僅需處理「A 當 1 或 11」的 21點常用規則（不需列舉所有組合，只要結果正確）

**驗收例**
- A + 7 = 18
- A + 7 + 9 = 17（A 必須變 1）
- A + A + 9 = 21（11 + 1 + 9）

---

### FR-5：Log（簡單敘述）
- 頁面需有一塊文字區域（例如 `<ul>` 或 `<div>`）按時間順序顯示：
  - 開局發牌
  - 玩家 Hit/Stand
  - 莊家自動補牌
  - 爆牌或最終結果

**格式**
- 不限語言，但需清楚可讀（建議中文）
- 最新訊息可在最上或最下，需一致

---

## 6. UI 規格（UI Requirements）
### 6.1 介面區塊
頁面至少包含以下區塊：
1. **標題**：21 點 / Blackjack
2. **莊家區**
   - 牌面列表（文字即可，例如 `A♠`, `10♥`，或使用簡單樣式卡片）
   - 點數顯示（玩家回合時可只顯示明牌點數或顯示 `?`；翻牌後顯示完整）
3. **玩家區**
   - 牌面列表
   - 點數顯示
4. **控制按鈕**
   - `New Game`
   - `Hit`
   - `Stand`
5. **結果區**
   - 顯示「進行中 / 玩家勝 / 莊家勝 / Push / 玩家爆牌 / 莊家爆牌」
6. **Log 區**
   - 顯示動作敘述

### 6.2 可用性/互動
- 遊戲進行中：
  - Hit、Stand 可用（除非玩家 BlackJack 或爆牌）
- 結算後：
  - Hit、Stand disabled
  - New Game 可用

---

## 7. 技術與實作限制（Constraints）
- **必須單檔 `index.html`**，不得引用外部 JS/CSS 檔案（可使用內嵌 `<style>`/`<script>`）
- 不依賴任何框架（純 Vanilla JS）
- 可離線開啟（file://）即可玩，不需伺服器
- 亂數來源：`Math.random()`（足夠做 demo）

---

## 8. 資料結構建議（Data Model Suggestions）
（非強制，但建議採用以利清楚）
- `deck: Card[]`
- `playerHand: Card[]`
- `dealerHand: Card[]`
- `state: "idle" | "playerTurn" | "dealerTurn" | "settled"`
- `log: string[]`

Card 建議格式：
- `{ suit: "♠"|"♥"|"♦"|"♣", rank: "A"|"2"|...|"10"|"J"|"Q"|"K" }`

---

## 9. 演算法/邏輯建議（Game Logic Notes）
- 洗牌：Fisher–Yates shuffle（推薦）
- 抽牌：從牌堆 `pop()` 取一張
- Blackjack 判定：
  - `hand.length === 2 && score(hand) === 21`
- 結算流程：
  1. 玩家爆牌 → dealer win
  2. 莊家爆牌 → player win
  3. 否則比點數 → win/lose/push

---

## 10. 驗收條件（Acceptance Criteria）
- [ ] 僅需一個 `index.html` 檔即可運行
- [ ] New Game 後能正常洗牌、發牌、顯示 UI
- [ ] Hit 能抽牌、更新點數、爆牌時正確結束
- [ ] Stand 能翻莊家暗牌、莊家依規則補牌並結算
- [ ] A 的 1/11 計分正確
- [ ] Log 能描述關鍵事件與最終結果
- [ ] 結算後 Hit/Stand 會被禁用，且可 New Game 重開

---

## 11. 未來擴充（Future Enhancements，非必做）
- 下注與籌碼、Blackjack 派彩
- Double/Split/Insurance/Surrender
- 多副牌與洗牌滲透率
- 多玩家與 AI 策略
- 儲存歷史局、重播、統計勝率
